---
summary: "CLI reference for `winclaw uninstall` (remove gateway service + local data)"
read_when:
  - You want to remove the gateway service and/or local state
  - You want a dry-run first
title: "uninstall"
---

# `winclaw uninstall`

Uninstall the gateway service + local data (CLI remains).

```bash
winclaw uninstall
winclaw uninstall --all --yes
winclaw uninstall --dry-run
```
