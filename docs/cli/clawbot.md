---
summary: "CLI reference for `winclaw clawbot` (legacy alias namespace)"
read_when:
  - You maintain older scripts using `winclaw clawbot ...`
  - You need migration guidance to current commands
title: "clawbot"
---

# `winclaw clawbot`

Legacy alias namespace kept for backwards compatibility.

Current supported alias:

- `winclaw clawbot qr` (same behavior as [`winclaw qr`](/cli/qr))

## Migration

Prefer modern top-level commands directly:

- `winclaw clawbot qr` -> `winclaw qr`
